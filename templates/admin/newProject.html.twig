{% extends 'base.html.twig' %}

{% block title %}Add New Project{% endblock %}

{% block body %}
<main>
    <div class="container">
        <div class="row">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit">Submit!</button>
            {{form_end(form)}}
        </div>
    </div>
</main>
{% endblock %}